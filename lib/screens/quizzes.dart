import 'package:flutter/material.dart';

class Quizzez extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('no quizzez yet'),
      ),
    );
  }
}
